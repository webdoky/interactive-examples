const paragraph = 'Гей, хлопці, не вспію - на ґанку ваша файна їжа знищується бурундучком. Певне справді файна!';

const searchTerm = 'файна';

console.log(`Індекс першого з кінця слова "${searchTerm}" дорівнює ${paragraph.lastIndexOf(searchTerm)}`);
// очікуваний вивід: "Індекс першого з кінця слова "файна" дорівнює 86"
