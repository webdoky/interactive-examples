const maxNumber = Math.pow(10, 1000); // Найбільше додатне число

if (maxNumber === Infinity) {
  console.log('Назвімо це Infinity!');
  // Очікуваний вивід: "Назвімо це Infinity!"
}

console.log(1 / maxNumber);
// Очікуваний вивід: 0
